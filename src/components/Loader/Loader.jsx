import { MoonLoader } from 'react-spinners';

export const Loader = () => {
    return (
      <>
        <MoonLoader
          color="#3f51b5"
          loading size={80}
        />
      </>
    );
}